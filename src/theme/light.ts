// light.ts
import { DefaultTheme } from 'styled-components';

const light: DefaultTheme = {
    borderRadius: '5px',

    colors: {
        main: 'cyan',
        secondary: 'magenta',
    },
};

export { light };
